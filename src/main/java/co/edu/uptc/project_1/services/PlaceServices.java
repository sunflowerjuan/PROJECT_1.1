package co.edu.uptc.project_1.services;

import co.edu.uptc.services.FileManager.FileManager;

public class PlaceServices {
    private String path = "places.txt";
    private FileManager fileManager = new FileManager(path);
}
